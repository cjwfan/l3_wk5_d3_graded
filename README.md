## What This Feature Does

The After Call Notes form lets an agent or user enter notes for a completed call in assigned fields, so the needed information is captured. After the form is submitted, the note is displayed on the page.

## How to Run the App

1. Install dependencies: npm install
2. Start the app: npm run dev 
3. Open the url shown in the terminal.
